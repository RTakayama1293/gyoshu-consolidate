# CLAUDE.md

## プロジェクト概要
- **目的**: 貨物営業部向け。業種別の企業CSV（53ファイル）を1つの統合リストに集約する
- **データ**: Sansanエクスポート形式のCSV。ファイル名が「大分類_小分類.csv」で業種を表す
- **成果物**: `outputs/統合_業種別企業リスト.csv`（および .xlsx）

---

## データ仕様

### 入力（data/raw/*.csv）
- 文字コード: UTF-8（BOM付き）
- ファイル名規則: `大分類_小分類.csv`（例: `漁業_水産養殖.csv`, `製造_業務用機械製造.csv`）
  - 大分類・小分類はファイル名から取得する（最初の `_` で分割）
- 主要列（元データ）:

| 元列名 | 用途 | 備考 |
|--------|------|------|
| 会社名 | → 社名 | 必須・100%充足 |
| 住所 | → 住所 | 100%充足。先頭から都道府県を判定 |
| 郵便番号 | → 郵便番号 | 100%充足 |
| 法人番号 | → 法人番号 | 100%充足。重複排除キーに使える |
| 代表者の氏名 | → 代表者名 | **ほぼ空欄（0.1%程度）**。Sansan未取得項目 |
| 電話番号 | → 代表電話 | **ほぼ空欄（0.1%程度）**。Sansan未取得項目 |

### 出力（outputs/統合_業種別企業リスト.csv）
列順: `大分類, 小分類, 社名, 地方, 都道府県, 郵便番号, 住所, 代表者名, 代表電話, 法人番号`
- 地方 / 都道府県 は住所から自動生成（`src/region_map.py` の8地方区分）

---

## 重要な前提・注意（no-estimation原則）
- **代表者名・代表電話は推定で埋めない**。元データが空ならそのまま空欄で出す
- 代表者名・代表電話はサンプルでほぼ全行空欄だった。53ファイル全体でも欠損前提で扱うこと
- 大分類・小分類は **ファイル名のみ** を正とする（CSV内の「主業：大分類」列は使わない方針。営業部の指示が「ファイル名で紐づく」のため）

---

## 実行方法

```bash
python src/consolidate.py
```

1. `data/raw/` の `*.csv` を全件読み込み
2. ファイル名から大分類・小分類を付与
3. 住所先頭から都道府県・地方を判定
4. `outputs/` にCSVとExcelを出力
5. 列ごとの充足率レポートをコンソール表示

---

## 利用可能なコマンド（CCWで指示する例）

| やりたいこと | 指示例 |
|-------------|--------|
| 統合実行 | 「src/consolidate.py を実行して」 |
| 充足率確認 | 「統合後の代表者名・代表電話の充足率を教えて」 |
| 重複確認 | 「法人番号で重複している企業がないか確認して」 |
| 地方別集計 | 「地方ごと・大分類ごとの件数を集計して」 |
| 沖縄を独立地方に | 「region_map.py の OKINAWA_AS_SEPARATE を True にして」 |

---

## ディレクトリルール
1. **data/raw/** - 元データ（53ファイル）格納。**絶対に編集しない**
2. **outputs/** - 統合結果
3. **src/** - 処理コード

---

## コーディング規約
- Python 3.x / pandas
- 型ヒント・docstring（Google形式）必須
- data/raw/ は不変（read-only）扱い
